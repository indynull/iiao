name = "algor-iiao"
main = "src/index.js"
compatibility_date = "2026-07-01"
workers_dev = true

# After first deploy, attach iiao.algor.ist in the dashboard
# or add a route once the zone is on Cloudflare:
# routes = [{ pattern = "iiao.algor.ist/*", zone_name = "algor.ist" }]
